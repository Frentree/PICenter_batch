<?xml version="1.0" encoding="EUC-KR"?>

<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"  "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="query">
	<!-- 계정 만료 1주일 전 회원  -->
	<select id="getAccountLockMember" resultClass="com.skyun.app.util.database.ibatis.vo.piLockMember">
		SELECT U.USER_NO, U.USER_NAME, U.USER_PHONE, U.LOGINDATE
		FROM pi_user U
		WHERE 1=1
			AND DATE_FORMAT(U.LOGINDATE, '%Y-%m-%d') = DATE_FORMAT(DATE_ADD(NOW(), INTERVAL 7 DAY), '%Y-%m-%d')
			AND U.LOGINDATE IS NOT NULL
			AND U.USER_GRADE IN (2,3)
			AND U.ACC_YN = 'Y'
	</select>

</sqlMap>
